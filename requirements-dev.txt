# gmail_zero_app — development and testing dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-mock>=3.12,<4.0
responses>=0.25,<1.0        # HTTP mocking for Gmail API calls
factory-boy>=3.3,<4.0       # Test fixture factories

# Linting / type checking
ruff>=0.4,<1.0
mypy>=1.9,<2.0