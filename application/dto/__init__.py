"""
Data Transfer Objects (DTOs) for gmail_zero_app.

DTOs cross the boundary between the presentation layer and the application
layer, carrying only the data needed for a specific operation.  They are
plain dataclasses with no behaviour.

Defined in Step 2:
    LabelOperationRequest  — parameters for a single label add/remove
    BulkLabelOperationRequest — parameters for bulk label operations
"""
