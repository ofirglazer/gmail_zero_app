"""
Scheduler sub-package for gmail_zero_app.

Optional APScheduler wrapper that triggers incremental sync on a configurable
schedule (default: daily at 08:00 local time).

The scheduler is disabled in demo mode and in test environments.

Implemented in Step 5.
"""
